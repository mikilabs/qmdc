## Spec [[spec1: Section]]

- source: docs

### Syntax [[syntax1: text]]

Example with table block:

```table
sql: SELECT __id FROM objects
scope: workspace
```

Without filtering:

```table
sql: SELECT __id FROM objects
scope: all
```

That is all.

